import psycopg2
import os

conn = psycopg2.connect("dbname=mydatabase user=user password=password host=localhost port=5432")



def criarImageForProductId():
    raw = 'INSERT INTO table (url, id) '


def geralLog():
    raw = 'INSERT INTO'

